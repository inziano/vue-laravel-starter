import Router from 'vue-router'

const router = new Router({
    mode: 'history',
    routes: [
        {
            path: '/',
            component: HTMLFormElement,
            name: ''
        }
    ]
})

export default router